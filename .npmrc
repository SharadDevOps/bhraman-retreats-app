# Flat, hoisted node_modules (like npm). Required so the Next.js standalone
# bundle resolves transitive deps (e.g. styled-jsx) after App Service tars and
# re-extracts node_modules — pnpm's default symlinked layout breaks there.
node-linker=hoisted
